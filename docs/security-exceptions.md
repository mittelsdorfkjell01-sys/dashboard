# Security exceptions

There are currently no accepted dependency-security exceptions.

Both Python and frontend dependency audits must pass. The frontend CI gate is
`high`; an advisory may only be temporarily accepted here when the affected
runtime path is demonstrably absent, no fixed release exists, an owner and
expiry date are recorded, and compensating controls are documented.
